server:
  port: 9001

spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        server-addr: master:8848

#端口暴漏
management:
  endpoints:
    web:
      exposure:
        include: '*'